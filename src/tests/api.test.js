import { describe, it, expect } from "vitest";

describe('describe el titulo lo que quiero testear', () => {
    it('descripcion del test unitario', () => {
        expect(true).toBe(true);
    })
});